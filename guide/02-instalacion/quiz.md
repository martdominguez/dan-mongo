# Quiz

## Preguntas de repaso

1. Cual es la diferencia entre el servidor de MongoDB y `mongosh`.

2. Por que `mongosh` es una herramienta importante para un desarrollador backend, incluso si luego va a usar Spring Boot.

3. En que situaciones MongoDB Compass puede ayudar y por que sigue siendo opcional.

4. Cual es la URI local mas comun para conectarte a una instancia de MongoDB en desarrollo.

5. Que comando usas en `mongosh` para cambiar a una base llamada `app_dev`.

6. Que comando usas para saber en que base estas trabajando.

7. Que comando usas para crear una coleccion llamada `users`.

8. Por que una base puede no aparecer todavia en `show dbs` aunque hayas usado `use`.

9. Que ventaja tiene validar la conexion con `mongosh` antes de depurar una aplicacion backend.

10. Menciona una secuencia minima de pasos para comprobar que el entorno local ya esta listo.

## Autoevaluacion breve

Si puedes responder con claridad estas tres afirmaciones, el objetivo del modulo esta cumplido:

- Entiendo que servidor, shell y herramienta visual no cumplen el mismo rol.
- Puedo conectarme con `mongosh` y hacer una validacion minima del entorno.
- Puedo crear una base y una coleccion simple antes de integrar MongoDB con una aplicacion backend.
